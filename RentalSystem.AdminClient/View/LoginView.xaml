<UserControl x:Class="RentalSystem.AdminClient.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RentalSystem.AdminClient"
             Width="400" Height="320">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!--TODO: Make this more beautiful-->

        <TextBlock Text="Admin Login" FontSize="22" FontWeight="Bold" Grid.Row="0"
                   HorizontalAlignment="Center" Margin="0 0 0 20"/>
        
        <TextBox Grid.Row="1" Margin="0 5"
                 Width="250"
                 HorizontalAlignment="Center"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Setter Property="Foreground" Value="Black"/>
                </Style>
            </TextBox.Style>
        </TextBox>

        <!-- den som vi bruger i fremtiden til auth
        <PasswordBox Grid.Row="2"
                     Margin="0 5"
                     Width="250"
                     x:Name="PasswordBox"
                     HorizontalAlignment="Center"/>
                     -->
       <!-- dummy pw box -->
        
        <PasswordBox Grid.Row="2"
                     Margin="0 5"
                     Width="250"
                     x:Name="PasswordBox"
                     PasswordChanged="PasswordBox_OnChanged"
                     HorizontalAlignment="Center"/>


        <Button Grid.Row="3" Content="Login"
                Width="120" Height="40"
                HorizontalAlignment="Center" Margin="0 15"
                Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=PasswordBox, Path=Password}" />

        <TextBlock Grid.Row="4" Text="{Binding LoginMessage}"
                   Foreground="Red"
                   HorizontalAlignment="Center" Margin="0 10"/>
    </Grid>
</UserControl>